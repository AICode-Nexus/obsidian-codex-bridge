# Roadmap

## v0.1 Stabilization

- Keep local `codex exec` and OpenAI Responses API backends stable.
- Improve diagnostics for local command execution from GUI-launched Obsidian.
- Preserve the guarded edit flow: suggest, preview, then apply.

## Later

- Add streaming progress in the side panel.
- Add cancellation for long-running local tasks.
- Consider ACP as an optional backend for persistent sessions, permission requests, and richer agent event streams.
